# Phase B Notebook Standardization

## Objective

- Keep every graded notebook pair in a consistent submission format while leaving the underlying ML code, datasets, and saved artifacts untouched.
- Use markdown-only notebook revisions to align the final repository with the course rubric.

## Final Submission Notebook Sections

1. Problem Framing
2. Data Acquisition, Preparation & Exploration
3. Modeling & Feature Selection
4. Evaluation & Interpretation
5. Causal and Relationship Analysis
6. Deployment Notes

## Notes On Scope

- The shared notebook factory created the original notebook scaffolds.
- The final submission notebooks in `ml/ml-pipelines/` were then expanded through markdown-only edits so the canonical files read like complete deliverables rather than like prompts.
- Supporting shared materials remain in `ml/docs/`, `ml/models/`, and `ml/reports/`.

## Shared References Used Across Notebook Pairs

- `ml/docs/data-joins.md`
- `ml/docs/feature-catalog.md`
- `ml/docs/phase-2-shared-prep.md`
- `ml/docs/phase-3-predictive-pipelines.md`
- `ml/docs/phase-4-modeling-framework.md`
- `ml/docs/phase-5-delivery-and-integration.md`

## Submission Positioning

- Not every notebook pair has the same maturity level. The top-level index now distinguishes artifact-backed pipeline notebooks from supplemental exploratory notebook pairs.
- Archival duplicate notebook names are explicitly marked `DO NOT SUBMIT` inside their own files.
